{
  "name": "root",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*",
      "packages/@dicebear/*"
    ]
  },
  "scripts": {
    "build": "lerna run build",
    "test": "lerna run test",
    "prepare-publish": "node scripts/version.mjs"
  },
  "devDependencies": {
    "lerna": "^8.1.2"
  }
}
