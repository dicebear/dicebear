{
  "name": "root",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*",
      "packages/@dicebear/*"
    ]
  },
  "scripts": {
    "build": "lerna run build",
    "test": "lerna run test",
    "prepare-publish": "lerna version --force-publish --exact"
  },
  "devDependencies": {
    "lerna": "^8.1.2"
  }
}
