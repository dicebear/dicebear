{
  "name": "root",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/@dicebear/*",
      "scripts",
      "website"
    ]
  },
  "scripts": {
    "build": "lerna run build",
    "test": "lerna run test",
    "publish": "lerna publish --force-publish"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
