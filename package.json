{
  "name": "root",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*",
      "packages/@dicebear/*"
    ]
  },
  "scripts": {
    "build": "lerna run build",
    "test": "lerna run test",
    "prepare-publish": "lerna version --force-publish"
  },
  "devDependencies": {
    "lerna": "^6.4.0"
  }
}
