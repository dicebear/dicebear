{
  "name": "root",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*",
      "packages/@dicebear/*",
      "plugins/figma/*",
      "scripts",
      "website"
    ]
  },
  "scripts": {
    "build": "lerna run build",
    "test": "lerna run test",
    "prepare-publish": "lerna version --force-publish ",
    "make-publish": "lerna publish from-package --force-publish"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
