{
  "name": "root",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*",
      "packages/@dicebear/*",
      "plugins/figma/*",
      "website"
    ]
  },
  "scripts": {
    "build": "lerna run build",
    "test": "lerna run test",
    "prepare-publish": "lerna version --force-publish"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
