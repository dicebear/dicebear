export const DOWNLOAD_AVATAR_SIZE = 512;
// The raster formats come from the HTTP API, which stops at 256 px and
// answers with 128 px when no size is asked for.
export const RASTER_DOWNLOAD_SIZE = 256;
export const DIALOG_PREVIEW_AVATAR_SIZE = 128;
export const MAX_CUSTOM_STYLE_UPLOAD_BYTES = 1024 * 1024;
